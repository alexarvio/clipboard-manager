node_modules
dist
src-tauri/target
.DS_Store

# Local agent/tooling artifacts -- not app code, nothing here should ship
.claude
.agents
.neon
skills-lock.json

# Auto-updater signing key (see docs/auto-updates.md) -- the private half
# must never be committed. Only the public key belongs in tauri.conf.json.
*.key
*.key.pub

# design-sync: staged converter scripts, build output, and machine state --
# see .design-sync/config.json / NOTES.md for what's durable vs. regenerated.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
